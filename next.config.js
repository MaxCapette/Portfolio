/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['yourdomain.com'], // Ajoutez ici le domaine de votre déploiement si nécessaire
    },
  };
  
  module.exports = nextConfig;
  